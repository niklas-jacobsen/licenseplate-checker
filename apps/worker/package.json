{
  "name": "@licenseplate-checker/worker",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@licenseplate-checker/shared": "*",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
